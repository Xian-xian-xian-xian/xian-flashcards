# Repository workflow

- After every code or configuration change, commit the intended files and push the current branch to its configured remote before reporting completion.
- Do not include unrelated or untracked files in a commit unless the user explicitly requests them.
